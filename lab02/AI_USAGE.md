# Опис використання генеративного ШІ (AI USAGE)

* **Інструмент:** ChatGPT / Gemini / Claude.
* **Завдання:** 
  1. Генерація базового шаблону коду Python (Scikit-Learn Pipeline, Cross-Validation, Matplotlib візуалізації).
  2. Форматування та допомога у структуруванні файлу `README.md` відповідно до заданого регламенту.
* **Перевірка:** Весь згенерований код і розраховані метрики перевірено та локально протестовано вручну.
# AI USAGE (Етап 2)

* **Завдання:** Генерація коду для стратифікованого розбиття `train_test_split` та форматування таблиці перевірки балансу класів у `README.md`.
* **Перевірка:** Параметри розділення та відсутність витоку даних перевірено вручну.
# AI USAGE (Етапи 3-4)

* **Завдання:** Створення коду для `Pipeline` з `StandardScaler` та `DummyClassifier`, налаштування метрик для `cross_validate`, генерація обґрунтування про запобігання Data Leakage та метрику Accuracy для `README.md`.
* **Перевірка:** Розрахунки метрик крос-валідації та правильність логіки пайплайнів перевірено вручну.

# AI USAGE (Етап 5)

* **Завдання:** Написання коду для оцінки моделей `LogisticRegression` та `GaussianNB` через 5-fold Stratified CV у `Pipeline` та генерація зведеної порівняльної таблиці для `README.md`.
* **Перевірка:** Коректність використання `Pipeline` та відповідність обчислених метрик перевірено вручну.

# AI USAGE (Етап 6)

* **Завдання:** Написання коду фінального навчання моделі `LogisticRegression` на `X_train`, розрахунку `predict` / `predict_proba` на `X_test` та опис текстового обґрунтування вибору моделі для `README.md`.
* **Перевірка:** Коректність викликів методів та формат масивів передбачень перевірено вручну.